---
"@execbox/core": minor
---

Require `openMcpToolProvider()` for local `{ server }` MCP sources, close owned MCP resources on setup failure, and make transport-backed sessions terminate promptly when the caller aborts.
